#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* _strcpy- print every other character
* @dest: pointer a
* @src: integer n
* Return: char pointer
*/
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
