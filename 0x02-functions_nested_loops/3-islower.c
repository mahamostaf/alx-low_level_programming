#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * print_alphabet_x10 - print alphapetical characters 10 times
 *
 * Return: void
 */
int _islower(int c)
{
    if (islower(c))
    {
        return (1);
    }
    else
    {
        return (0);
    }
}
