#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * islower - check if islower case or not
 *
 * Return: 0 or 1
 */
int _islower(int c)
{
if (islower(c))
{
return (1);
}
else
{
return (0);
}
}
